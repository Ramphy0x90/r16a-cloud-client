import { IconFromExtensionPipe } from './icon-from-extension-pipe';

describe('IconFromExtensionPipe', () => {
  it('create an instance', () => {
    const pipe = new IconFromExtensionPipe();
    expect(pipe).toBeTruthy();
  });
});
